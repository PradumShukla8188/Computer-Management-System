export enum ExamType {
    INTERNAL = 'INTERNAL',
    FINAL = 'FINAL',
}

export enum ExamMode {
    OBJECTIVE = 'OBJECTIVE',
    SUBJECTIVE = 'SUBJECTIVE',
}

export enum QuestionType {
    MCQ = 'MCQ',
    TYPING = 'TYPING',
}
