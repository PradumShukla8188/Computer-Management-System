export interface ApiMethods {
    POST: string;
    GET: string;
    DELETE: string;
    PUT: string;
    PATCH: string;
}

export const apiMethods: ApiMethods = {
    POST: "post",
    GET: "get",
    DELETE: "delete",
    PUT: "put",
    PATCH: "patch",
};
